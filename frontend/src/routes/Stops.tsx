import { Box, Heading } from "@chakra-ui/react";

const Stops = () => {
  return (
    <div>
      <Box p={4}>
        <Heading as={"h2"}>Stops</Heading>
      </Box>
    </div>
  );
};

export default Stops;
